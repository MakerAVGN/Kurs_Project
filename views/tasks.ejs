<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Details</title>
    <link rel="stylesheet" href="/css/cabinet.css" />
</head>

<body>

    <body>
        <%- include('navigation') %>
            <%- include('topbar') %>
                <!—=========================Main====================—>
                        <div class="taskContainer" >
                                <% if (tasksInfo.length> 0) { %>
                                    <div class="task-text">
                                    <h2>
                                        <%= tasksInfo[questionID].taskName %>
                                    </h2>
                                    <p>Points: <%= tasksInfo[questionID].points %>
                                    </p>
                                    </div>


                                    <div class="text-description">
                                    <p>
                                        <%= tasksInfo[questionID].taskDescription %>
                                    </p>
                                    </div>
                                    <form class="task-form"
                                        action="<%= (questionID === tasksInfo.length - 1) ? `/cabinet/courses/${courseID}/submit-answers` : `/cabinet/courses/${courseID}/next-question` %>"
                                        method="POST">

                                        <% const options=JSON.parse(tasksInfo[questionID].options); %>
                                            <% options.options.forEach((option, index)=> { %>
                                            <div class="task-answers">
                                                <label>
                                                    <input type="radio" name="answer" value="<%= index %>" required>
                                                    <%= option %>
                                                </label>
                                            </div>
                                                <% }); %>
                                            <div class="task-btn-container">
                                                    <button type="submit">
                                                        <%= (questionID===tasksInfo.length - 1) ? "Submit Answers"
                                                            : "Next Question" %>
                                                    </button>
                                                </div>
                                    </form>
                                    <% } else { %>
                                        <p>No tasks available.</p>
                                        <% } %>
                                        <!-- <div class="task-text">
                                            <h2 >
                                                Английский язык 60+
                                            </h2>
                                            <p>Points: 5</p>
                                        </div>
                                        <div class="text-description">
                                            <p>
                                                Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptates, cum eos labore ab voluptatum recusandae vel laboriosam expedita reiciendis blanditiis quaerat nihil aliquid veritatis nam aliquam architecto? Amet, repudiandae debitis!
                                            </p>   
                                        </div>
                                        <form class="task-form" action="">
                                            <div class="task-answers">
                                                <label>
                                                    <input type="radio" name="answer">
                                                    Ответ 1
                                                </label>
                                                  <label>
                                                    <input type="radio" name="answer">
                                                    Ответ 2
                                                  </label>
                                                  <label>
                                                    <input type="radio" name="answer">
                                                    Ответ 3
                                                  </label>
                                                  <label>
                                                    <input type="radio" name="answer">
                                                    Ответ 4
                                                </label>
                                                <label>
                                                    <input type="radio" name="answer">
                                                    Ответ 5
                                                </label>
                                            </div>
                                            <div class="task-btn-container">
                                                <button type="submit" class="task-btn"> Ответить</button>
                                            </div>
                                        </form>
                                           
                        </div> -->  


    </body>
<script>
    document.addEventListener('DOMContentLoaded', function() {
  const labels = document.querySelectorAll('.task-form label');

  labels.forEach(function(label) {
    label.addEventListener('click', function() {
      // Удалить класс 'selected' у всех label
      labels.forEach(function(otherLabel) {
        otherLabel.classList.remove('selected');
      });

      // Добавить класс 'selected' к текущему label
      label.classList.add('selected');

      // Пометить соответствующий input как выбранный
      const input = label.querySelector('input');
      input.checked = true;
    });
  });
});
</script>
</html>